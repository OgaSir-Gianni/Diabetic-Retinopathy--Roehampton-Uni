# data is re-downloadable — never commit it; results (metrics, figures) are
# committed so markers can see them, but model weights stay out
data/
*.pt
*.zip

# secrets
kaggle.json
.kaggle/

# python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# os
.DS_Store

# local tool config
.claude/

# working report drafts stay local (submitted via Turnitin, not the repo)
report/*.docx
